[project]
name = "react-agent-learning"
version = "0.1.0"
description = "手写 ReAct Agent（30 天计划 Day 16）：纯 Python 实现 Thought→Action→Observation 循环"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.40",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = [
    "debugpy>=1.8",
]
